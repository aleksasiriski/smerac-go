---
# Configuration file for smerac-go
#
# Copy this sample to /config/smerac.yml and replace the various attributes
# with the values for your environment. For more details please see the README.

# Discord configuration
discord:
    token: supersecrettoken #! this field is required, either via config or env var

# Roles configuration
roles:
    #- id: "Discord ID"
    #  name: "Name of the role"

# Calendar configuration
calendars:
    #- apikey: "API key for Google calendar"
    #  name: "Name of the calendar" # best to correspond to a role name
    #  time: 3 # number of hours between the checks if the calendar has been updated